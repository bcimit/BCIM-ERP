// Shared typography scale — use these constants for new pages/components and
// when touching existing ones, so headings/labels/table headers are
// consistent across modules instead of each page picking its own size/weight/color.
//
// Pairs with the Zoho-style form constants in `zohoStyles.js`
// (Z_INP / Z_LABEL / Z_CARD / Z_HEAD).

export const PAGE_TITLE = 'text-xl font-semibold text-slate-800';
export const SECTION_HEADER = 'text-sm font-semibold text-slate-800 uppercase tracking-wide';
export const CARD_HEAD = 'text-[13px] font-semibold text-slate-700';
export const FIELD_LABEL = 'text-xs font-medium text-slate-600';
export const TABLE_HEADER = 'text-[11px] font-medium uppercase tracking-wider text-slate-400';
export const BODY = 'text-sm text-slate-700';
export const META = 'text-xs text-slate-400';
